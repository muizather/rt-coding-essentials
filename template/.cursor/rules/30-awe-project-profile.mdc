---
description: AWE project profile — generated by awe-setup from your answers. Always applies. Edit values here as the project evolves; setup never overwrites it.
alwaysApply: true
---

# AWE Project Profile — {{PROJECT_NAME}}

Generated by `awe-setup` on {{SETUP_DATE}}. These values drive the hooks, skills, and agents. Keep them accurate — the stop-evidence gate runs `commands.test`, and the ship gate pushes only `awe/<ticket>-*` branches cut from the base branch below.

## Project

- **Name:** {{PROJECT_NAME}}
- **Base branch:** `{{BASE_BRANCH}}` — all `awe/<ticket>-*` branches are cut from here and PRs target here.
- **Roles in play:** {{ROLES}}
- **Ticket system:** {{TICKET_SYSTEM}} (intake pulls tickets via the matching MCP when configured; otherwise paste ticket text manually)

## Commands

- **Test:** `{{TEST_COMMAND}}` — must exit 0 on success; its result is written to `.cursor/state/awe-evidence.json`.
- **Lint:** `{{LINT_COMMAND}}` — run before every review round.

## Pipeline tuning

- **Review iterations:** {{REVIEW_ITERATIONS}} — needs-fix rounds per ticket before human escalation.
- **Strict security mode:** {{STRICT_SECURITY}} — when `true`, missing scanners fail hard gates closed instead of warning.
- **Trigger mode:** manual (run `/awe-*` skills yourself). To automate transitions, wire Cursor Automations to the skills after setup — see README § Cloud & team usage.
- **Notifications:** OFF by default. Optional Slack/Gmail notifications are good-to-haves; enable via `.cursor/mcp.json` + `notifications` in `awe.config.json`.

## Environment URLs

{{ENV_URLS}}

## Deploy commands

{{DEPLOY_COMMANDS}}
